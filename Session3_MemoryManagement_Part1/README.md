## Memory layout
    stack - allocated automatically
    data - aloocated for global and static variable
    heap - allocated at runtime
All memory is taken from the process address space

## Struct and class
- Keep in mind the alignment of member, "flexibale array member not at the end of class/struct" can arise
#include <iostream>
using namespace std;

// Function declaration or prototype
// Note: Even though function declaration allow us to omit parameter name, we should specify a name to improve readablity.
int sampleFunction(int);
void sampleFunctionVoid(int);